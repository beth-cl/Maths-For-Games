using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class W3_usenewmethods : MonoBehaviour
{
    MyVector3 eulerRotation = new MyVector3(0f, 0f, 0f);
    MyVector3 forwardDirection = new MyVector3(0f, 0f, 0f);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //eulerRotation = CalculateEuler();
        
    }
}
